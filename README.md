Sources for the plugin `shared.android.support.v7.appcompat`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.android.support.v7.appcompat" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
